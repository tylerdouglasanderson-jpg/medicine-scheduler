{
  "name": "med-scheduler",
  "version": "0.4.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.mjs",
    "test": "vitest run",
    "smoke": "node scripts/smoketest.mjs"
  },
  "dependencies": { "exceljs": "^4.4.0", "highs": "^1.0.0" },
  "devDependencies": { "jsdom": "^24.0.0", "vite": "^5.0.0", "vite-plugin-singlefile": "^2.0.0", "vitest": "^1.6.0" }
}
